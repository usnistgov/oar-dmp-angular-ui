export interface SoftwareDevelopment {
  development: string; // yes | no
  softwareUse: string; // Internal NIST Use | External Use | Both
  softwareDatabase: string; // yes | no
  softwareWebsite: string // yes | no
}
