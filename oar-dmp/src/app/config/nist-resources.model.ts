export interface NIST_Resources {
  /**
   * RESOURCES is lson type object that determines the resources that NIST has to offer when creating a DMP
   */
  RESOURCES: any;

  /**
   * other parameters are allowed
   */
  [paramName: string]: any;
}