import { ResourceOptionsDirective } from './resource-options.directive';

describe('ResourceOptionsDirective', () => {
  it('should create an instance', () => {
    const directive = new ResourceOptionsDirective();
    expect(directive).toBeTruthy();
  });
});
